#!/urs/bin/ptython3
"""[lookup]"""


def lookup(obj):
    """[lookup function]

    Arguments:
        obj {[type]} -- [Returns the list of available
                        attributes and methods of an object:]
    """
    return (dir(obj))
